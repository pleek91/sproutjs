{
  "name": "en-us",
  "version": "1.0.0",
  "author": "pleek91",
  "scripts": {
    "dev": "tsc -w & tsc-alias -w & vite build -w",
    "build": "tsc && tsc-alias && vite build",
    "test": "vitest",
    "lint": "eslint ./",
    "lint:fix": "eslint ./ --fix",
    "validate:type": "vue-tsc --noEmit"
  },
  "dependencies": {
    "tsc-alias": "^1.8.5",
    "typescript": "^5.0.4",
    "vite": "^4.2.1",
    "vitest": "^0.30.1"
  }
}
